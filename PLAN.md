- having pub history_mode: HistoryMode,
  pub history_days: Option<u32>,
  is kinda bad... maybe we do it as a Option<> NonzeroU32 like data_retention_days:
- we'll have to rething entity / global filters a bit.. lets check out competitors maybe
- prob wanna allow disabling screen dimensions too?
- links to docs everywhere
- maybe reorganize stuff a bit, display settings for projects are a bit confusing rn
